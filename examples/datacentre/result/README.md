This is the flattened composed example, including its derived opinions and presentation; open it from the example directory with `usdview result/example.usdc` (no family plugins or sibling checkouts needed). The example's own authored layers are `layers/inputs/cameras.usda`, `layers/out/axis.derived.usda`, `layers/out/review.usda`, `layers/out/wall.drivers.derived.usda`, `layers/out/wall.drivers.usda`. Source pin: `usdaeco-datacentre v0.4.8`, variant `clash`; source mode `pinned` (a minimal or override run does not prove the pinned release). The parent manifest records source hashes; pinned source layers are not copied here. `vanilla.png` records a stock USD render of this result.
