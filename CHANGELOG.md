# Changelog

## 0.2.5

- public re-pin: usdaeco-toolchain v0.3.10, usdaeco-core v0.9.5,
  usdaeco-axis v0.1.5, usdaeco-buildup v0.2.5, usdaeco-ifc v0.2.2
  and usdaeco-datacentre v0.4.8; record each checked revision.
- Retain supported library requirement ranges; require toolchain >=0.3.10,<0.4
  for the release-tag and package-version checks.
- Republish the example with refreshed provenance and producer stamps;
  preserve schema, geometry, driver values and findings.

## 0.2.4

- public names → github.com/criad-com; use toolchain v0.3.8.
- Refresh the example's toolchain pin and producer metadata; retain the
  published crate, images, source data and all other dependency pins.

## 0.2.3

- Use toolchain v0.3.6 and S29 to route published review-layer sources through
  the example's ignored inputs/source link, independent of checkout layout.
- Republish the same composed example with portable source references.


## 0.2.2

- Re-pin to train aeco-0.7.0; retain supported requirement ranges and
  declare historical compatibility fixtures separately.
- Refresh the stale example for the data-centre clash tessellation and
  tolerance updates, axis v0.1.2 stamps and the patch producer version.

## 0.2.1

- Publish the flattened, self-contained wall example, its editable layers
  and a stock USD render through toolchain v0.3.1.
- Verify committed results with S27/S28 and require all eight core
  validators to load and detect a seeded identity defect.
- Adopt MIT licensing and record the pending shared-lint compatibility.

## 0.2.0

- Adopt the flat family skeleton, core v0.9.1 and the shared axis library;
  preserve the v0.1.2 wall/opening property contract.
- Discover six Python UsdValidation rules through `usdAecoWallValidators`.
  Error names are ProperCase; old severity-profile spellings remain accepted.
- Add `aeco-wall import`, `check` and `validators`. Keep the positional IFC
  importer; separate promoted derived values from driver opinions.
- Add stage-only SI-property promotion, marking width-derived sections as
  equivalent single layers and retaining absent topology as absent.
- Publish the Route K use case and pinned clash-variant corner example,
  with manifest-checked source data, L01 axes and a CPU render.
- Move source-run tests to `testenv/`; add previous-tag schema comparison,
  importer regression coverage, userDoc and Apache-2.0 licensing.

## 0.1.2

- Retain the released wall promotion, six validation rules and profile
  checks as the migration's 46-check floor.
